# Additional dependencies for running the test suite.
#
#     pip install -r requirements.txt -r requirements-dev.txt
#     pytest            # runs the tests (uses a virtual X display via pytest-xvfb)

-r requirements.txt

pytest==9.1.1
pytest-qt==4.5.0
pytest-xvfb==3.1.1         # runs the GUI tests headless (needs the `xvfb` system package)
pytest-mock==3.15.1
pytest-repeat==0.9.4
